# RULES.project · 本项目铁律（每项目不同，填这里）

> OS 通用铁律见 `RULES.md`(勿动)。这里只写**本项目**特有的红线。
> CLAUDE.md 会把新 agent 指到这份。

<!-- 格式·防跑偏 | 结构型(项目填):固定三块——项目红线 / 致命错误即停工 / 性能·数据标准(可选)。
照 <...> 占位填本项目特有红线;OS 通用铁律不写这里(在 RULES.md)。 -->

## 项目红线
- <冻结文件 / 不可改的页面或契约，如 `frontend/.../XxxPage` 冻结,只可排版/显示/加字段>
- <资产/范围硬约束，如 某市场永不实盘 / 禁 import 某类库>
- <安全不变量(开发不得削弱,由产品代码运行时强制)，如 实盘 key 不进 LLM / 杠杆护栏 / 权限边界>
- <单一源锚点，如 身份源在 `app/.../ids.py`,不另造>

## 致命错误即停工（本项目定义，对应 RULES.md §5）
- <列出本项目「出现即停工」的具体错误,如 look-ahead 泄露 / 动钱路径绕过护栏 / 实盘 key 外泄>

## 性能 / 数据标准（可选）
- <如:某查询 < Xs / 每表 ≥N data tests + 版本 + checksum / 可复现 ±1e-6>
